<?php

	
	
	echo $form->create();
	echo $form->input("username", "", array() );
	echo $form->input("password", "", array("type"=>"password") );
	echo $form->close("Entrar");
	
	
	if(isset($authError)):
		 echo "Login incorreto. Tente novamente.";
		 //echo $authError;
	endif;

?>